# BackupMesh

BackupMesh coordinates incremental backup capture, leases, durable manifest publication, verification receipts, deterministic retries, retention protection, and gap-preserving restore cursors. The replica snapshot integrity package remains the chunk and manifest foundation; the orchestration packages add durable cross-component workflows around it.

Run `go test ./...`, `go build ./...`, and `go vet ./...` from this directory.
